if (id) {
    foreach(num in numeros) {
        while (true) {
            switch (num) {
                case 1:
                    llamadita();
                case 2:
                    break;
                default:
                    jiji("jiji");
                    break;

            }
        }
    }
}